{>"layouts/master" /}

{<pageHeader}
{/pageHeader}

{<pageContent}
        <div ng-controller="PostListCtrl">
            <div masonry>
                <div class="masonry-brick item w2" ng-repeat="post in posts">
                    <img ng-src="{{ post.content }}" alt="A masonry brick">
                </div>
            </div>
        </div>
{/pageContent}
